# Руководство по настройке разовой покупки God Mode за $1

## Обзор

Это руководство объясняет, как настроить разовую покупку (Non-Consumable In-App Purchase) для разблокировки God Mode в вашем iOS приложении за $1.

---

## Шаг 1: Создание продукта в App Store Connect

### 1.1 Войдите в App Store Connect

1. Перейдите на [App Store Connect](https://appstoreconnect.apple.com)
2. Войдите со своим Apple Developer аккаунтом
3. Выберите ваше приложение "2048 God mode"

### 1.2 Создайте In-App Purchase

1. В боковом меню приложения выберите **"Функции"** (Features)
2. Нажмите **"+** рядом с **"In-App Purchases"**
3. Выберите тип покупки: **"Non-Consumable"** (Непотребляемая покупка)
   - Этот тип идеален для разблокировки функций один раз навсегда

### 1.3 Настройте продукт

Заполните следующие поля:

- **Reference Name**: `God Mode Unlock` (только для вас, не видно пользователям)
- **Product ID**: `com.kolechenkov.2048godmode.godmode` (должен быть уникальным)
- **Price**: Выберите **$0.99** (Tier 1) или **$1.00** (если доступно в вашей стране)

### 1.4 Добавьте локализацию

Нажмите **"Add Localization"** и добавьте информацию для каждого языка:

**Для английского (или основного языка):**
- **Display Name**: `God Mode`
- **Description**: `Unlock God Mode to manipulate tiles freely. Move, swap, spawn, and delete tiles at will.`

**Для русского (если поддерживаете):**
- **Display Name**: `Режим Бога`
- **Description**: `Разблокируйте режим Бога для свободного управления плитками. Перемещайте, меняйте местами, создавайте и удаляйте плитки по желанию.`

### 1.5 Сохраните продукт

1. Нажмите **"Save"** в правом верхнем углу
2. Статус будет **"Ready to Submit"** после заполнения всех обязательных полей
3. **Важно**: Продукт должен быть отправлен на модерацию вместе с приложением или после его публикации

---

## Шаг 2: Установка плагина In-App Purchase

### 2.1 Установите плагин

```bash
npm install @capacitor-community/in-app-purchase
```

### 2.2 Синхронизируйте с iOS

```bash
npx cap sync ios
```

### 2.3 Откройте проект в Xcode

```bash
npx cap open ios
```

---

## Шаг 3: Настройка кода приложения

### 3.1 Создайте сервис для покупок

Создайте файл `services/purchaseService.ts`:

```typescript
import { InAppPurchase } from '@capacitor-community/in-app-purchase';

const GOD_MODE_PRODUCT_ID = 'com.kolechenkov.2048godmode.godmode';

export const purchaseService = {
  // Инициализация покупок
  async initialize(): Promise<void> {
    try {
      await InAppPurchase.initialize();
    } catch (error) {
      console.error('Failed to initialize purchases:', error);
    }
  },

  // Получить информацию о продукте
  async getProductInfo(): Promise<any> {
    try {
      const products = await InAppPurchase.getProducts({
        productIds: [GOD_MODE_PRODUCT_ID],
        productType: 'nonconsumable',
      });
      return products.products[0];
    } catch (error) {
      console.error('Failed to get product info:', error);
      return null;
    }
  },

  // Проверить, куплен ли God Mode
  async isPurchased(): Promise<boolean> {
    try {
      const purchases = await InAppPurchase.getPurchases();
      return purchases.purchases.some(
        p => p.productId === GOD_MODE_PRODUCT_ID
      );
    } catch (error) {
      console.error('Failed to check purchases:', error);
      return false;
    }
  },

  // Купить God Mode
  async purchase(): Promise<boolean> {
    try {
      const result = await InAppPurchase.purchase({
        productId: GOD_MODE_PRODUCT_ID,
        productType: 'nonconsumable',
      });
      return result.transactionReceipt !== undefined;
    } catch (error) {
      console.error('Purchase failed:', error);
      return false;
    }
  },

  // Восстановить покупки (если пользователь переустановил приложение)
  async restorePurchases(): Promise<boolean> {
    try {
      await InAppPurchase.restorePurchases();
      return await this.isPurchased();
    } catch (error) {
      console.error('Failed to restore purchases:', error);
      return false;
    }
  },
};
```

### 3.2 Обновите App.tsx

В вашем `App.tsx` добавьте:

```typescript
import { useEffect, useState } from 'react';
import { purchaseService } from './services/purchaseService';

const App: React.FC = () => {
  const [godModePurchased, setGodModePurchased] = useState(false);
  const [godMode, setGodMode] = useState(false);
  const [purchasing, setPurchasing] = useState(false);
  const [productInfo, setProductInfo] = useState<any>(null);

  // Инициализация при загрузке
  useEffect(() => {
    const init = async () => {
      await purchaseService.initialize();
      
      // Проверить, куплен ли уже God Mode
      const purchased = await purchaseService.isPurchased();
      setGodModePurchased(purchased);
      
      // Получить информацию о продукте (цена и т.д.)
      const product = await purchaseService.getProductInfo();
      setProductInfo(product);
    };
    
    init();
  }, []);

  // Обработка покупки
  const handlePurchase = async () => {
    setPurchasing(true);
    const success = await purchaseService.purchase();
    
    if (success) {
      setGodModePurchased(true);
      setGodMode(true); // Автоматически включить после покупки
    }
    
    setPurchasing(false);
  };

  // Восстановление покупок
  const handleRestore = async () => {
    const restored = await purchaseService.restorePurchases();
    if (restored) {
      setGodModePurchased(true);
    }
  };

  // God Mode можно включать только если куплен
  const toggleGodMode = () => {
    if (godModePurchased) {
      setGodMode(!godMode);
    }
  };

  return (
    // ... ваш существующий код ...
    
    // В секции God Mode замените переключатель на:
    {!godModePurchased ? (
      <div>
        <button 
          onClick={handlePurchase}
          disabled={purchasing || !productInfo}
          className="px-6 py-3 bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl font-bold"
        >
          {purchasing 
            ? 'Обработка...' 
            : `Купить God Mode за ${productInfo?.price || '$1.00'}`}
        </button>
        <button 
          onClick={handleRestore}
          className="text-sm text-slate-400 hover:text-slate-300 mt-2"
        >
          Восстановить покупки
        </button>
      </div>
    ) : (
      // Ваш существующий переключатель God Mode
      <button onClick={toggleGodMode}>
        {/* существующий код переключателя */}
      </button>
    )}
  );
};
```

---

## Шаг 4: Тестирование покупок

### 4.1 Создайте Sandbox тестовый аккаунт

1. В App Store Connect перейдите в **"Users and Access"**
2. Выберите **"Sandbox Testers"**
3. Создайте новый тестовый аккаунт (используйте реальный email, но не тот, что привязан к Apple ID)
4. Запишите логин и пароль

### 4.2 Выйдите из App Store на устройстве

**Важно**: Перед тестированием выйдите из App Store на тестовом устройстве:
- Настройки → [Ваше имя] → Медиа и покупки → Выйти

### 4.3 Запустите приложение

1. Соберите и установите приложение на устройство через Xcode
2. При попытке покупки iOS попросит войти в App Store
3. Используйте данные **Sandbox тестового аккаунта**
4. Покупка будет бесплатной (но симулирует реальную транзакцию)

### 4.4 Проверьте покупку

- После покупки God Mode должен быть разблокирован
- Попробуйте переустановить приложение и использовать "Восстановить покупки"
- Проверьте, что покупка сохраняется между сессиями

---

## Шаг 5: Публикация

### 5.1 Отправка продукта на модерацию

1. В App Store Connect перейдите к вашему приложению
2. Перейдите в **"In-App Purchases"**
3. Выберите ваш продукт "God Mode"
4. Нажмите **"Submit for Review"**
5. Заполните необходимую информацию (если требуется)

### 5.2 Включение в релиз приложения

При отправке новой версии приложения на модерацию:
1. Убедитесь, что продукт In-App Purchase находится в статусе **"Ready to Submit"** или **"Waiting for Review"**
2. При отправке приложения на модерацию Apple автоматически увидит связанные продукты

---

## Частые проблемы и решения

### Проблема: "Product not found"

**Решение**:
- Убедитесь, что Product ID точно совпадает в коде и App Store Connect
- Проверьте, что продукт сохранен в App Store Connect
- Убедитесь, что используете правильный тип продукта (`nonconsumable`)

### Проблема: Покупка не проходит в Sandbox

**Решение**:
- Убедитесь, что вышли из App Store на устройстве
- Проверьте, что используете Sandbox тестовый аккаунт
- Убедитесь, что продукт имеет статус "Ready to Submit"

### Проблема: Покупка не восстанавливается

**Решение**:
- Вызовите `restorePurchases()` при запуске приложения
- Проверьте, что используете правильный Product ID
- Убедитесь, что покупка была завершена успешно

### Проблема: Цена отображается неправильно

**Решение**:
- Проверьте, что получили информацию о продукте через `getProductInfo()`
- Убедитесь, что локализация продукта настроена правильно

---

## Важные замечания

1. **Non-Consumable покупки**: Этот тип покупки покупается один раз и разблокируется навсегда для пользователя (даже при переустановке приложения)

2. **Восстановление покупок**: Всегда предоставляйте кнопку "Восстановить покупки" для пользователей, которые переустановили приложение

3. **Проверка при запуске**: Проверяйте статус покупки при каждом запуске приложения

4. **Локальное кэширование**: Можно дополнительно сохранять статус покупки в `localStorage` для быстрого доступа, но всегда проверяйте через API StoreKit

5. **Модерация**: Продукт In-App Purchase должен быть одобрен Apple вместе с приложением или отдельно

---

## Дополнительные ресурсы

- [Apple In-App Purchase Documentation](https://developer.apple.com/in-app-purchase/)
- [Capacitor In-App Purchase Plugin](https://github.com/capacitor-community/in-app-purchase)
- [App Store Review Guidelines - In-App Purchase](https://developer.apple.com/app-store/review/guidelines/#in-app-purchase)

---

## Чек-лист перед публикацией

- [ ] Продукт создан в App Store Connect
- [ ] Product ID настроен правильно
- [ ] Цена установлена ($0.99 или $1.00)
- [ ] Локализации добавлены (минимум английский)
- [ ] Плагин установлен и синхронизирован
- [ ] Код покупок реализован
- [ ] Тестирование в Sandbox прошло успешно
- [ ] Восстановление покупок работает
- [ ] Продукт отправлен на модерацию
- [ ] Кнопка "Восстановить покупки" добавлена в UI

---

**Удачи с реализацией покупок! 💰**



